[build]
  command   = "npm run build"
  publish   = "dist"

# SPA fallback — all routes served by index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

# Security & performance headers
[[headers]]
  for = "/*"
  [headers.values]
    # Prevent our pages from being embedded in iframes on other domains
    X-Frame-Options        = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy        = "strict-origin-when-cross-origin"
    # Note: no frame-src CSP is set here so the Google Maps 360° modal iframe
    # (google.com/maps/embed) can load freely inside our own pages.
    Permissions-Policy     = "camera=(), microphone=(), geolocation=()"

# Long-term cache for hashed assets (JS/CSS bundles from Vite)
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# No cache for index.html so deploys are always fresh
[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
